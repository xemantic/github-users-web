I had to compile `incremental-dom` from resent sources in order to package
it using `js-closure` target, which adds:

    goog.module('incrementaldom');

Source: https://github.com/google/incremental-dom
